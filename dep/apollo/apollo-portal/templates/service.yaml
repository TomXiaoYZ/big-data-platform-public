apiVersion: v1
kind: Service
metadata:
  name: {{ include "apollo-portal.fullname" . }}
  labels:
    {{- include "apollo-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apollo-portal.selectorLabels" . | nindent 4 }}
